// Explore helpful Math library functions.
const values = [2.3, 9.9, -4.1];

console.log("Floor:", values.map(Math.floor));
console.log("Ceil:", values.map(Math.ceil));
console.log("Absolute values:", values.map(Math.abs));