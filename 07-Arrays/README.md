# Arrays in JavaScript

Arrays are used to store multiple values in a single variable.

## Creating Arrays

### Array Literal
`const arr = [1, 2, 3];`

### Array Constructor
`const arr = new Array(1, 2, 3);`

## Array Properties

### length
Returns the number of elements in an array

## Common Array Methods

### Adding/Removing Elements
- `push()` - Add to end
- `pop()` - Remove from end
- `unshift()` - Add to beginning
- `shift()` - Remove from beginning
- `splice()` - Add/remove at specific position

### Searching
- `indexOf()` - Find index of element
- `includes()` - Check if element exists
- `find()` - Find first matching element
- `findIndex()` - Find index of first match

### Iteration
- `forEach()` - Execute function for each element
- `map()` - Create new array with results
- `filter()` - Create array with elements that pass test
- `reduce()` - Reduce array to single value

### Other Methods
- `slice()` - Extract section of array
- `concat()` - Merge arrays
- `join()` - Join elements into string
- `reverse()` - Reverse array
- `sort()` - Sort array

## Examples
Check the example files in this folder to see arrays in action!
