// Highlight the difference between null and undefined values.
let futureValue;
const emptyValue = null;

console.log("futureValue:", futureValue);
console.log("emptyValue:", emptyValue);
console.log("futureValue equals emptyValue:", futureValue == emptyValue);
console.log("futureValue strictly equals emptyValue:", futureValue === emptyValue);